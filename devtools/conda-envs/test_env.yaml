name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

#idk man
  - numpy
  - networkx
  - math
  - random

    # Pip-only installs
  #- pip:
  #  - codecov

